<!DOCTYPE html>
<html>
  <head>
    <title>PostAd</title>
    <%= csrf_meta_tags %>
    
    <%# Using Bootstrap CDN version 3.3 %>
    <%= stylesheet_link_tag 'https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css' %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  
  
  <body>

    <%# Using Bootstrap for the nav bar %>
    <nav class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header"> 
          <button type="button" class="navbar-toggle collapsed"
            data-toggle="collapse" data-target="#navbar"
            aria-expanded="false" aria-controls="navbar">

            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        
          <a class="navbar-brand">PostAds</a>
        </div>
      
      
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li><%= link_to "Home", home_path %></li>
                <%# need to define home in routes.rb %>
          </ul>
        
          <ul class="nav navbar-nav navbar-right">
          
            <% if signed_in? %>
                <li><a style="color:#000066"><%= 'Signed in as: ' + current_user.email %></a></li>
                <li><%= link_to 'Sign out', sign_out_path, method: :delete %></li>
              <% else %>
                <li><%= link_to 'Sign in', sign_in_path %></li>
            <% end %>
          
            <li><%= link_to "Create Post", new_post_path %></li>
          </ul>
        </div>
      
      </div>
    </nav>




    <%# Put our page contents in bootstrap container %>
    <div class="container">
    
      <%# Sign in messages %>
      <div id="flash">
        <% flash.each do |key, value| %>
          <div class="flash <%= key %>"><%= value %></div>
        <% end %>
      </div>
    
      <%= yield %>
    </div>



  </body>
</html>
